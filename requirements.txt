# === Core LDA & ML Stack (User Constraints) ===
# These are locked to the versions you need for legacy compatibility.
pandas==1.5.3
numpy==1.24.4
scikit-learn==1.2.2
scipy==1.10.1
gensim==4.3.3
pyLDAvis==3.4.0

# === Streamlit App & Analysis Stack ===
# Rolling back to 1.32.2 to avoid the protobuf<5 vs >=5.26.1 conflict
streamlit==1.32.2
plotly==5.22.0

# === NLTK Dependency Fix ===
# Fix for 'label-studio-sdk' which needs nltk >= 3.8.2
nltk==3.9.1